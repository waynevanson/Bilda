# a comment!
let
  a = 1
  b = 2
  c = 3
  d = { inherit a b c }
  e = on list::map
        single = [1 2 3 4]
      do
        double = f * 2
      in
        { single double }
  f = (
    name: String
    value: Int
  ) => "{name}-{value}"
in {
 value = {
    d = value
    o = []
 }
}
